Building SIPp on Ubuntu/Precise
===============================

Build the package without dpkg-source complaining about the .git
directory here::

    ( tmpdst=/tmp/deb-$(stat -c%i .);
      mv debian/.git $tmpdst && dpkg-buildpackage -us -uc -sa;
      ret=$?; mv $tmpdst debian/.git; exit $ret )
